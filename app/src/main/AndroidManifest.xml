<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lh200445635.comp3025_final_project">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COMP3025FinalProject">

        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->

        <activity
            android:name="com.lh200445635.comp3025_final_project.SigninActivity"
            android:label="@string/title_activity_signin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <activity
            android:name="com.lh200445635.comp3025_final_project.AddRecipeActivity"
            android:label="@string/title_activity_add_recipe"></activity>
        <activity
            android:name="com.lh200445635.comp3025_final_project.RecipeActivity"
            android:label="@string/title_activity_recipe" />
        <activity
            android:name="com.lh200445635.comp3025_final_project.RecipeListActivity"
            android:label="@string/title_activity_recipe_list"></activity>
        <activity android:name="com.lh200445635.comp3025_final_project.MainActivity">
        </activity>
    </application>

</manifest>